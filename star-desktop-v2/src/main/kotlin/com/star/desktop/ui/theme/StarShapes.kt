package com.star.desktop.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/** Corner radii recovered from the macOS app's views. */
object StarShapes {
    val card = RoundedCornerShape(6.dp)
    val infoBox = RoundedCornerShape(4.dp)
    val picker = RoundedCornerShape(5.dp)
    val button = RoundedCornerShape(6.dp)
    val gridCell = RoundedCornerShape(2.dp)
    val closeConfirmCard = RoundedCornerShape(16.dp)
    val errorCard = RoundedCornerShape(20.dp)
}
