import XCTest
@testable import decision_tree_generator

final class decision_tree_generatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(decision_tree_generator().text, "Hello, World!")
    }
}
