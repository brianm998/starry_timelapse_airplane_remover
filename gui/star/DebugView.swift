import SwiftUI

struct DebugView: View {
    var body: some View {
        Text("DEBUG")
        // add logs, log level selection, clear, etc
        // add number of images in cache
        // enable file logging?
          .navigationTitle("Star Debug")
    }
}
